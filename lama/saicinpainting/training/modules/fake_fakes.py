# Stubbed out — FakeFakesGenerator is only used during training.
# Original code imported removed kornia API (SamplePadding).

class FakeFakesGenerator:
    def __init__(self, *args, **kwargs):
        raise NotImplementedError("FakeFakesGenerator is a training-only stub")
